const sqlite3 = require('sqlite3').verbose();

// Amazon.db नावाचा DB create करा
const db = new sqlite3.Database('Amazon.db', (err) => {
  if(err) console.error(err.message);
  else console.log('Connected to Amazon.db');
});

// Products table तयार करा
db.serialize(()=>{
  db.run(`CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    price REAL NOT NULL,
    image TEXT NOT NULL
  )`, err=>{
    if(err) console.error(err.message);
    else console.log("Products table created successfully!");
  });
});

// Close DB
// db.close();